name: GetScreen Windows (4H)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Downloading & Setting Up
        run: |
         echo "EMAIL_SECRET=aerouni46@gmail.com" > secrets.txt
         Invoke-WebRequest -Uri "https://www.dropbox.com/scl/fi/y9oameg2cisozic1cn2bp/down.bat?rlkey=k4vydzk9a9hddroazmyrqjtzr&dl=0" -OutFile "down.bat"
         cmd /c down.bat

      - name: Time Counter
        run: python time.py
