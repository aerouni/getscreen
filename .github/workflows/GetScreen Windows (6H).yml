name: GetScreen Windows (6H)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Downloading & Setting Up
        run: |
         echo "EMAIL_SECRET=aerouni46@gmail.com" > secrets.txt
         Invoke-WebRequest -Uri "https://www.dropbox.com/scl/fi/8fefde1rdifkwrq3xdpd6/down.bat?rlkey=xn749whg0157s5p1bg9bawljz&dl=0" -OutFile "down.bat"
         cmd /c down.bat

      - name: Time Counter
        run: python time.py
