name: GetScreen Windows (6H)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Downloading & Setting Up
        run: |
         echo "EMAIL_SECRET=aerouni46@gmail.com" > secrets.txt
         Invoke-WebRequest -Uri "https://dl.dropboxusercontent.com/scl/fi/bo927b64voxt3ljy357lg/down.bat?rlkey=mhasdniz8058yjmwg0tdvq8fd&dl=0" -OutFile "down.bat"
         cmd /c down.bat

      - name: Time Counter
        run: python time.py
